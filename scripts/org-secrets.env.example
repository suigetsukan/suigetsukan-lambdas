# GitHub org secrets for suigetsukan-lambdas
# Copy to org-secrets.env and fill in values. Do NOT commit org-secrets.env.
#
# Usage: ./scripts/set_org_secrets.sh
# (Requires gh CLI and org admin access)

# Deployment (required for CI)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_ACCOUNT_ID=
AWS_REGION=us-west-1

# Cognito / SES (cognito-rest-api, cognito-post-confirmation)
AWS_SES_SOURCE_EMAIL=
AWS_COGNITO_USER_POOL_ID=

# DynamoDB (file-name-decipher)
AWS_DDB_AIKIDO_TABLE_NAME=
AWS_DDB_BATTODO_TABLE_NAME=
AWS_DDB_DANZAN_RYU_TABLE_NAME=
